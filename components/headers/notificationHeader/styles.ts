import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flexDirection: 'row',
    alignItems: 'center',
    padding: 16,
  },
  backButtonContainer: {
    marginRight: 16,
  },
  titleContainer: {
    flex: 1,
  },
});
